GROQ_API_KEY=your_groq_api_key_here
# Optional: a GitHub personal access token (no scopes needed, just used to
# authenticate) so services/github_service.py gets GitHub's 5,000 req/hour
# authenticated rate limit instead of the 60 req/hour unauthenticated one.
GITHUB_TOKEN=
